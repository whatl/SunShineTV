# ==================== Docker 构建配置 ====================
# 此文件仅用于 Docker 镜像构建时传递构建参数
# 运行时配置请使用 .env.deploy.example

# 站点名称
NEXT_PUBLIC_SITE_NAME=AAA影视
# 微软数据统计
NEXT_PUBLIC_CLARITY_ID=tlfxbqjaur
# 后端API服务器
NEXT_PUBLIC_MACCMS_API_BASE='http://aaays.com'

# 数据源是maccms
NEXT_PUBLIC_DATA_SOURCE=maccms
# 不显示视频数据过滤标题
NEXT_PUBLIC_SHOW_FILTER_TOOLBAR=false
# 不支持流式搜索
NEXT_PUBLIC_FLUID_SEARCH=false
# 十八禁过滤
NEXT_PUBLIC_DISABLE_YELLOW_FILTER=false

# ⚠️ 注意：
# NEXT_PUBLIC_STORAGE_TYPE 和 KVROCKS_URL 不在构建时设置
# 这些配置在运行时通过 docker-compose.yml 的 .env 文件注入

